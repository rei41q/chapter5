function additonal(x, y){ //function2
    return x+y;
}

const addition = (x,y) =>{ // function1
    return x+y;
}

const substrack = (x, y) =>{
    return x - y;
}

module.exports = {addition, substrack};